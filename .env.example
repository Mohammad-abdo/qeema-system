# Backend API base URL (required). No trailing slash. Vite only exposes vars prefixed with VITE_
# Use this so the React app receives data from the backend (dashboard, projects, tasks, etc.)
VITE_API_URL=http://127.0.0.1:4000
# Alternative name (same usage):
# VITE_PROJECTS_BACKEND_URL=http://127.0.0.1:4000
